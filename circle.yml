machine:
  node:
    version: 6.9.1

dependencies:
  pre:
    - npm install -g typescript mocha https://github.com/PeculiarVentures/rollem.git

test:

general:
  branches:
    ignore:
      - gh-pages
